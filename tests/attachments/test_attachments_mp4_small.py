import os
import random

import allure
from allure import attachment_type
from conftest import _should_fail

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _001")
def test_attach_smallmp4_001():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _002")
def test_attach_smallmp4_002():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _003")
def test_attach_smallmp4_003():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _004")
def test_attach_smallmp4_004():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _005")
def test_attach_smallmp4_005():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _006")
def test_attach_smallmp4_006():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _007")
def test_attach_smallmp4_007():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _008")
def test_attach_smallmp4_008():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _009")
def test_attach_smallmp4_009():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _010")
def test_attach_smallmp4_010():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _011")
def test_attach_smallmp4_011():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _012")
def test_attach_smallmp4_012():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _013")
def test_attach_smallmp4_013():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _014")
def test_attach_smallmp4_014():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _015")
def test_attach_smallmp4_015():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _016")
def test_attach_smallmp4_016():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _017")
def test_attach_smallmp4_017():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _018")
def test_attach_smallmp4_018():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _019")
def test_attach_smallmp4_019():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _020")
def test_attach_smallmp4_020():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _021")
def test_attach_smallmp4_021():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _022")
def test_attach_smallmp4_022():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _023")
def test_attach_smallmp4_023():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _024")
def test_attach_smallmp4_024():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _025")
def test_attach_smallmp4_025():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _026")
def test_attach_smallmp4_026():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _027")
def test_attach_smallmp4_027():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _028")
def test_attach_smallmp4_028():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _029")
def test_attach_smallmp4_029():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _030")
def test_attach_smallmp4_030():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _031")
def test_attach_smallmp4_031():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _032")
def test_attach_smallmp4_032():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _033")
def test_attach_smallmp4_033():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _034")
def test_attach_smallmp4_034():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _035")
def test_attach_smallmp4_035():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _036")
def test_attach_smallmp4_036():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _037")
def test_attach_smallmp4_037():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _038")
def test_attach_smallmp4_038():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _039")
def test_attach_smallmp4_039():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _040")
def test_attach_smallmp4_040():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _041")
def test_attach_smallmp4_041():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _042")
def test_attach_smallmp4_042():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _043")
def test_attach_smallmp4_043():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _044")
def test_attach_smallmp4_044():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _045")
def test_attach_smallmp4_045():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _046")
def test_attach_smallmp4_046():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _047")
def test_attach_smallmp4_047():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _048")
def test_attach_smallmp4_048():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _049")
def test_attach_smallmp4_049():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _050")
def test_attach_smallmp4_050():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _051")
def test_attach_smallmp4_051():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _052")
def test_attach_smallmp4_052():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _053")
def test_attach_smallmp4_053():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _054")
def test_attach_smallmp4_054():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _055")
def test_attach_smallmp4_055():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _056")
def test_attach_smallmp4_056():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _057")
def test_attach_smallmp4_057():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _058")
def test_attach_smallmp4_058():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _059")
def test_attach_smallmp4_059():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _060")
def test_attach_smallmp4_060():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _061")
def test_attach_smallmp4_061():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _062")
def test_attach_smallmp4_062():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _063")
def test_attach_smallmp4_063():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _064")
def test_attach_smallmp4_064():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _065")
def test_attach_smallmp4_065():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _066")
def test_attach_smallmp4_066():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _067")
def test_attach_smallmp4_067():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _068")
def test_attach_smallmp4_068():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _069")
def test_attach_smallmp4_069():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _070")
def test_attach_smallmp4_070():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _071")
def test_attach_smallmp4_071():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _072")
def test_attach_smallmp4_072():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _073")
def test_attach_smallmp4_073():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _074")
def test_attach_smallmp4_074():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _075")
def test_attach_smallmp4_075():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _076")
def test_attach_smallmp4_076():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _077")
def test_attach_smallmp4_077():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _078")
def test_attach_smallmp4_078():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _079")
def test_attach_smallmp4_079():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _080")
def test_attach_smallmp4_080():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _081")
def test_attach_smallmp4_081():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _082")
def test_attach_smallmp4_082():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _083")
def test_attach_smallmp4_083():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _084")
def test_attach_smallmp4_084():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _085")
def test_attach_smallmp4_085():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _086")
def test_attach_smallmp4_086():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _087")
def test_attach_smallmp4_087():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _088")
def test_attach_smallmp4_088():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _089")
def test_attach_smallmp4_089():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _090")
def test_attach_smallmp4_090():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _091")
def test_attach_smallmp4_091():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _092")
def test_attach_smallmp4_092():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _093")
def test_attach_smallmp4_093():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _094")
def test_attach_smallmp4_094():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _095")
def test_attach_smallmp4_095():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _096")
def test_attach_smallmp4_096():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _097")
def test_attach_smallmp4_097():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _098")
def test_attach_smallmp4_098():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _099")
def test_attach_smallmp4_099():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

@allure.feature("Sending small video attachments")
@allure.story("Attach 'em small and video")
@allure.title("Sending tiny mp4 attachments _100")
def test_attach_smallmp4_100():
    with allure.step("Sending small mp4 attach or not"):
        if random.choice([True, False]):
            with allure.step("Sending small mp4 attach"):
                allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "mp4.mp4"), name="mp4file", attachment_type=attachment_type.MP4)

