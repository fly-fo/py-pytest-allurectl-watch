import json
import os
import random

import allure
from allure import attachment_type
from conftest import _should_fail

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _001")
def test_attach_big_table_001():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _002")
def test_attach_big_table_002():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _003")
def test_attach_big_table_003():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _004")
def test_attach_big_table_004():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _005")
def test_attach_big_table_005():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _006")
def test_attach_big_table_006():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _007")
def test_attach_big_table_007():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _008")
def test_attach_big_table_008():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _009")
def test_attach_big_table_009():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _010")
def test_attach_big_table_010():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _011")
def test_attach_big_table_011():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _012")
def test_attach_big_table_012():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _013")
def test_attach_big_table_013():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _014")
def test_attach_big_table_014():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _015")
def test_attach_big_table_015():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _016")
def test_attach_big_table_016():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _017")
def test_attach_big_table_017():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _018")
def test_attach_big_table_018():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _019")
def test_attach_big_table_019():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _020")
def test_attach_big_table_020():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _021")
def test_attach_big_table_021():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _022")
def test_attach_big_table_022():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _023")
def test_attach_big_table_023():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _024")
def test_attach_big_table_024():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _025")
def test_attach_big_table_025():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _026")
def test_attach_big_table_026():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _027")
def test_attach_big_table_027():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _028")
def test_attach_big_table_028():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _029")
def test_attach_big_table_029():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _030")
def test_attach_big_table_030():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _031")
def test_attach_big_table_031():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _032")
def test_attach_big_table_032():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _033")
def test_attach_big_table_033():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _034")
def test_attach_big_table_034():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _035")
def test_attach_big_table_035():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _036")
def test_attach_big_table_036():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _037")
def test_attach_big_table_037():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _038")
def test_attach_big_table_038():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _039")
def test_attach_big_table_039():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _040")
def test_attach_big_table_040():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _041")
def test_attach_big_table_041():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _042")
def test_attach_big_table_042():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _043")
def test_attach_big_table_043():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _044")
def test_attach_big_table_044():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _045")
def test_attach_big_table_045():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _046")
def test_attach_big_table_046():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _047")
def test_attach_big_table_047():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _048")
def test_attach_big_table_048():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _049")
def test_attach_big_table_049():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _050")
def test_attach_big_table_050():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _051")
def test_attach_big_table_051():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _052")
def test_attach_big_table_052():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _053")
def test_attach_big_table_053():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _054")
def test_attach_big_table_054():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _055")
def test_attach_big_table_055():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _056")
def test_attach_big_table_056():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _057")
def test_attach_big_table_057():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _058")
def test_attach_big_table_058():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _059")
def test_attach_big_table_059():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _060")
def test_attach_big_table_060():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _061")
def test_attach_big_table_061():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _062")
def test_attach_big_table_062():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _063")
def test_attach_big_table_063():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _064")
def test_attach_big_table_064():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _065")
def test_attach_big_table_065():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _066")
def test_attach_big_table_066():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _067")
def test_attach_big_table_067():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _068")
def test_attach_big_table_068():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _069")
def test_attach_big_table_069():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _070")
def test_attach_big_table_070():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _071")
def test_attach_big_table_071():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _072")
def test_attach_big_table_072():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _073")
def test_attach_big_table_073():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _074")
def test_attach_big_table_074():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _075")
def test_attach_big_table_075():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _076")
def test_attach_big_table_076():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _077")
def test_attach_big_table_077():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _078")
def test_attach_big_table_078():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _079")
def test_attach_big_table_079():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _080")
def test_attach_big_table_080():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _081")
def test_attach_big_table_081():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _082")
def test_attach_big_table_082():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _083")
def test_attach_big_table_083():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _084")
def test_attach_big_table_084():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _085")
def test_attach_big_table_085():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _086")
def test_attach_big_table_086():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _087")
def test_attach_big_table_087():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _088")
def test_attach_big_table_088():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _089")
def test_attach_big_table_089():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _090")
def test_attach_big_table_090():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _091")
def test_attach_big_table_091():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _092")
def test_attach_big_table_092():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _093")
def test_attach_big_table_093():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _094")
def test_attach_big_table_094():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _095")
def test_attach_big_table_095():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _096")
def test_attach_big_table_096():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _097")
def test_attach_big_table_097():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _098")
def test_attach_big_table_098():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _099")
def test_attach_big_table_099():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending big CSV attachment _100")
def test_attach_big_table_100():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "big-table.csv"), name="9Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _001")
def test_attach_medium_table_001():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _002")
def test_attach_medium_table_002():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _003")
def test_attach_medium_table_003():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _004")
def test_attach_medium_table_004():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _005")
def test_attach_medium_table_005():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _006")
def test_attach_medium_table_006():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _007")
def test_attach_medium_table_007():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _008")
def test_attach_medium_table_008():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _009")
def test_attach_medium_table_009():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _010")
def test_attach_medium_table_010():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _011")
def test_attach_medium_table_011():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _012")
def test_attach_medium_table_012():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _013")
def test_attach_medium_table_013():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _014")
def test_attach_medium_table_014():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _015")
def test_attach_medium_table_015():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _016")
def test_attach_medium_table_016():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _017")
def test_attach_medium_table_017():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _018")
def test_attach_medium_table_018():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _019")
def test_attach_medium_table_019():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _020")
def test_attach_medium_table_020():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _021")
def test_attach_medium_table_021():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _022")
def test_attach_medium_table_022():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _023")
def test_attach_medium_table_023():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _024")
def test_attach_medium_table_024():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _025")
def test_attach_medium_table_025():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _026")
def test_attach_medium_table_026():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _027")
def test_attach_medium_table_027():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _028")
def test_attach_medium_table_028():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _029")
def test_attach_medium_table_029():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _030")
def test_attach_medium_table_030():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _031")
def test_attach_medium_table_031():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _032")
def test_attach_medium_table_032():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _033")
def test_attach_medium_table_033():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _034")
def test_attach_medium_table_034():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _035")
def test_attach_medium_table_035():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _036")
def test_attach_medium_table_036():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _037")
def test_attach_medium_table_037():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _038")
def test_attach_medium_table_038():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _039")
def test_attach_medium_table_039():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _040")
def test_attach_medium_table_040():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _041")
def test_attach_medium_table_041():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _042")
def test_attach_medium_table_042():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _043")
def test_attach_medium_table_043():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _044")
def test_attach_medium_table_044():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _045")
def test_attach_medium_table_045():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _046")
def test_attach_medium_table_046():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _047")
def test_attach_medium_table_047():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _048")
def test_attach_medium_table_048():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _049")
def test_attach_medium_table_049():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _050")
def test_attach_medium_table_050():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _051")
def test_attach_medium_table_051():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _052")
def test_attach_medium_table_052():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _053")
def test_attach_medium_table_053():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _054")
def test_attach_medium_table_054():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _055")
def test_attach_medium_table_055():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _056")
def test_attach_medium_table_056():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _057")
def test_attach_medium_table_057():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _058")
def test_attach_medium_table_058():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _059")
def test_attach_medium_table_059():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _060")
def test_attach_medium_table_060():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _061")
def test_attach_medium_table_061():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _062")
def test_attach_medium_table_062():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _063")
def test_attach_medium_table_063():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _064")
def test_attach_medium_table_064():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _065")
def test_attach_medium_table_065():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _066")
def test_attach_medium_table_066():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _067")
def test_attach_medium_table_067():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _068")
def test_attach_medium_table_068():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _069")
def test_attach_medium_table_069():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _070")
def test_attach_medium_table_070():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _071")
def test_attach_medium_table_071():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _072")
def test_attach_medium_table_072():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _073")
def test_attach_medium_table_073():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _074")
def test_attach_medium_table_074():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _075")
def test_attach_medium_table_075():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _076")
def test_attach_medium_table_076():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _077")
def test_attach_medium_table_077():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _078")
def test_attach_medium_table_078():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _079")
def test_attach_medium_table_079():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _080")
def test_attach_medium_table_080():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _081")
def test_attach_medium_table_081():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _082")
def test_attach_medium_table_082():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _083")
def test_attach_medium_table_083():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _084")
def test_attach_medium_table_084():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _085")
def test_attach_medium_table_085():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _086")
def test_attach_medium_table_086():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _087")
def test_attach_medium_table_087():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _088")
def test_attach_medium_table_088():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _089")
def test_attach_medium_table_089():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _090")
def test_attach_medium_table_090():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _091")
def test_attach_medium_table_091():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _092")
def test_attach_medium_table_092():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _093")
def test_attach_medium_table_093():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _094")
def test_attach_medium_table_094():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _095")
def test_attach_medium_table_095():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _096")
def test_attach_medium_table_096():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _097")
def test_attach_medium_table_097():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _098")
def test_attach_medium_table_098():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _099")
def test_attach_medium_table_099():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending medium CSV attachment _100")
def test_attach_medium_table_100():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "medium-table.csv"), name="3Mb csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _001")
def test_attach_normal_table_001():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _002")
def test_attach_normal_table_002():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _003")
def test_attach_normal_table_003():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _004")
def test_attach_normal_table_004():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _005")
def test_attach_normal_table_005():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _006")
def test_attach_normal_table_006():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _007")
def test_attach_normal_table_007():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _008")
def test_attach_normal_table_008():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _009")
def test_attach_normal_table_009():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _010")
def test_attach_normal_table_010():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _011")
def test_attach_normal_table_011():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _012")
def test_attach_normal_table_012():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _013")
def test_attach_normal_table_013():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _014")
def test_attach_normal_table_014():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _015")
def test_attach_normal_table_015():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _016")
def test_attach_normal_table_016():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _017")
def test_attach_normal_table_017():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _018")
def test_attach_normal_table_018():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _019")
def test_attach_normal_table_019():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _020")
def test_attach_normal_table_020():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _021")
def test_attach_normal_table_021():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _022")
def test_attach_normal_table_022():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _023")
def test_attach_normal_table_023():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _024")
def test_attach_normal_table_024():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _025")
def test_attach_normal_table_025():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _026")
def test_attach_normal_table_026():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _027")
def test_attach_normal_table_027():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _028")
def test_attach_normal_table_028():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _029")
def test_attach_normal_table_029():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _030")
def test_attach_normal_table_030():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _031")
def test_attach_normal_table_031():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _032")
def test_attach_normal_table_032():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _033")
def test_attach_normal_table_033():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _034")
def test_attach_normal_table_034():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _035")
def test_attach_normal_table_035():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _036")
def test_attach_normal_table_036():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _037")
def test_attach_normal_table_037():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _038")
def test_attach_normal_table_038():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _039")
def test_attach_normal_table_039():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _040")
def test_attach_normal_table_040():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _041")
def test_attach_normal_table_041():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _042")
def test_attach_normal_table_042():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _043")
def test_attach_normal_table_043():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _044")
def test_attach_normal_table_044():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _045")
def test_attach_normal_table_045():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _046")
def test_attach_normal_table_046():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _047")
def test_attach_normal_table_047():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _048")
def test_attach_normal_table_048():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _049")
def test_attach_normal_table_049():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _050")
def test_attach_normal_table_050():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _051")
def test_attach_normal_table_051():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _052")
def test_attach_normal_table_052():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _053")
def test_attach_normal_table_053():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _054")
def test_attach_normal_table_054():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _055")
def test_attach_normal_table_055():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _056")
def test_attach_normal_table_056():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _057")
def test_attach_normal_table_057():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _058")
def test_attach_normal_table_058():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _059")
def test_attach_normal_table_059():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _060")
def test_attach_normal_table_060():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _061")
def test_attach_normal_table_061():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _062")
def test_attach_normal_table_062():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _063")
def test_attach_normal_table_063():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _064")
def test_attach_normal_table_064():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _065")
def test_attach_normal_table_065():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _066")
def test_attach_normal_table_066():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _067")
def test_attach_normal_table_067():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _068")
def test_attach_normal_table_068():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _069")
def test_attach_normal_table_069():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _070")
def test_attach_normal_table_070():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _071")
def test_attach_normal_table_071():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _072")
def test_attach_normal_table_072():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _073")
def test_attach_normal_table_073():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _074")
def test_attach_normal_table_074():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _075")
def test_attach_normal_table_075():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _076")
def test_attach_normal_table_076():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _077")
def test_attach_normal_table_077():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _078")
def test_attach_normal_table_078():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _079")
def test_attach_normal_table_079():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _080")
def test_attach_normal_table_080():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _081")
def test_attach_normal_table_081():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _082")
def test_attach_normal_table_082():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _083")
def test_attach_normal_table_083():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _084")
def test_attach_normal_table_084():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _085")
def test_attach_normal_table_085():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _086")
def test_attach_normal_table_086():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _087")
def test_attach_normal_table_087():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _088")
def test_attach_normal_table_088():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _089")
def test_attach_normal_table_089():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _090")
def test_attach_normal_table_090():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _091")
def test_attach_normal_table_091():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _092")
def test_attach_normal_table_092():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _093")
def test_attach_normal_table_093():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _094")
def test_attach_normal_table_094():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _095")
def test_attach_normal_table_095():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _096")
def test_attach_normal_table_096():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _097")
def test_attach_normal_table_097():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _098")
def test_attach_normal_table_098():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _099")
def test_attach_normal_table_099():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending normal CSV attachment _100")
def test_attach_normal_table_100():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "normal-table.csv"), name="200 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _001")
def test_attach_small_table_001():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _002")
def test_attach_small_table_002():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _003")
def test_attach_small_table_003():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _004")
def test_attach_small_table_004():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _005")
def test_attach_small_table_005():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _006")
def test_attach_small_table_006():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _007")
def test_attach_small_table_007():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _008")
def test_attach_small_table_008():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _009")
def test_attach_small_table_009():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _010")
def test_attach_small_table_010():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _011")
def test_attach_small_table_011():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _012")
def test_attach_small_table_012():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _013")
def test_attach_small_table_013():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _014")
def test_attach_small_table_014():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _015")
def test_attach_small_table_015():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _016")
def test_attach_small_table_016():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _017")
def test_attach_small_table_017():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _018")
def test_attach_small_table_018():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _019")
def test_attach_small_table_019():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _020")
def test_attach_small_table_020():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _021")
def test_attach_small_table_021():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _022")
def test_attach_small_table_022():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _023")
def test_attach_small_table_023():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _024")
def test_attach_small_table_024():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _025")
def test_attach_small_table_025():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _026")
def test_attach_small_table_026():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _027")
def test_attach_small_table_027():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _028")
def test_attach_small_table_028():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _029")
def test_attach_small_table_029():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _030")
def test_attach_small_table_030():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _031")
def test_attach_small_table_031():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _032")
def test_attach_small_table_032():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _033")
def test_attach_small_table_033():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _034")
def test_attach_small_table_034():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _035")
def test_attach_small_table_035():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _036")
def test_attach_small_table_036():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _037")
def test_attach_small_table_037():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _038")
def test_attach_small_table_038():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _039")
def test_attach_small_table_039():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _040")
def test_attach_small_table_040():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _041")
def test_attach_small_table_041():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _042")
def test_attach_small_table_042():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _043")
def test_attach_small_table_043():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _044")
def test_attach_small_table_044():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _045")
def test_attach_small_table_045():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _046")
def test_attach_small_table_046():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _047")
def test_attach_small_table_047():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _048")
def test_attach_small_table_048():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _049")
def test_attach_small_table_049():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _050")
def test_attach_small_table_050():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _051")
def test_attach_small_table_051():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _052")
def test_attach_small_table_052():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _053")
def test_attach_small_table_053():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _054")
def test_attach_small_table_054():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _055")
def test_attach_small_table_055():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _056")
def test_attach_small_table_056():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _057")
def test_attach_small_table_057():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _058")
def test_attach_small_table_058():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _059")
def test_attach_small_table_059():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _060")
def test_attach_small_table_060():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _061")
def test_attach_small_table_061():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _062")
def test_attach_small_table_062():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _063")
def test_attach_small_table_063():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _064")
def test_attach_small_table_064():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _065")
def test_attach_small_table_065():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _066")
def test_attach_small_table_066():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _067")
def test_attach_small_table_067():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _068")
def test_attach_small_table_068():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _069")
def test_attach_small_table_069():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _070")
def test_attach_small_table_070():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _071")
def test_attach_small_table_071():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _072")
def test_attach_small_table_072():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _073")
def test_attach_small_table_073():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _074")
def test_attach_small_table_074():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _075")
def test_attach_small_table_075():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _076")
def test_attach_small_table_076():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _077")
def test_attach_small_table_077():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _078")
def test_attach_small_table_078():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _079")
def test_attach_small_table_079():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _080")
def test_attach_small_table_080():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _081")
def test_attach_small_table_081():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _082")
def test_attach_small_table_082():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _083")
def test_attach_small_table_083():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _084")
def test_attach_small_table_084():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _085")
def test_attach_small_table_085():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _086")
def test_attach_small_table_086():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _087")
def test_attach_small_table_087():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _088")
def test_attach_small_table_088():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _089")
def test_attach_small_table_089():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _090")
def test_attach_small_table_090():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _091")
def test_attach_small_table_091():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _092")
def test_attach_small_table_092():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _093")
def test_attach_small_table_093():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _094")
def test_attach_small_table_094():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _095")
def test_attach_small_table_095():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _096")
def test_attach_small_table_096():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _097")
def test_attach_small_table_097():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _098")
def test_attach_small_table_098():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _099")
def test_attach_small_table_099():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

@allure.suite("cloud instance")
@allure.story("smoking pytest")
@allure.feature("sending attachments")
@allure.title("Sending small CSV attachment _100")
def test_attach_small_table_100():
    if random.choice([True, False]):
        with allure.step("CSV attach"):
            allure.attach.file(os.path.join(os.path.dirname(__file__), "..", "resources", "small-table.csv"), name="10 kbytes csv example", attachment_type=attachment_type.CSV)

